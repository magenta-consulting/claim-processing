

{% extends 'SonataAdminBundle:CRUD:base_show_field.html.twig' %}

{% block field %}
        {% set value = getLimitAmount(object,object.position) %}
        {% if value is not null %}
                {{ value|number_format(2, '.', ',') }} {{ getCurrencyDefault() }}
        {% endif %}
{% endblock %}

